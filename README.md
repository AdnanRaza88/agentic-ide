**Implemented**: foundation infra + Universal Model Provider System (`packages/providers`) + Graph Engine (`packages/graph`) + Persistent State and Session Subsystem (`packages/state`).

**Not implemented yet** (by design): agent runtime, MCP, plugins, sandbox, full UI, etc.
