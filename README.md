**Implemented**: foundation infra + Universal Model Provider System (`packages/providers`) + Graph Engine (`packages/graph`).

**Not implemented yet** (by design): agent runtime, MCP, plugins, sandbox, full UI, etc.
