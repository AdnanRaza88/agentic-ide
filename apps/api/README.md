# API (`apps/api`)

FastAPI application that exposes the Agent Runtime over HTTP and WebSocket.

Implementation will begin after contracts are stable.

See `packages/contracts/api/` for the contract surface.
