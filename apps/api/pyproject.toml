[project]
name = "agentic-ide-api"
version = "0.1.0"
description = "FastAPI backend for Agentic IDE"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.0",
]
